import{render,screen}from'@testing-library/react-native';import DiscoverScreen from'@/app/(tabs)/index';describe('<DiscoverScreen />',()=>{test('makes the KARE decision promise the primary action',()=>{render(<DiscoverScreen/>);expect(screen.getByRole('header',{name:'Bu gece ne izleyeceğini düşünme.'})).toBeTruthy();expect(screen.getByText('NE İZLESEM?')).toBeTruthy()})});
