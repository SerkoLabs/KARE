import{rankMovies}from'../engine';import type{Movie,RecommendationInput}from'../../movies/domain';const movie=(id:number,genres=['Drama']):Movie=>({tmdbId:id,title:`Film ${id}`,overview:'Özet',runtimeMinutes:115,genres,keywords:[],originCountries:[],posterUrl:'https://image.tmdb.org/t/p/w500/x.jpg',voteAverage:8,voteCount:1000,cast:[]});const input:RecommendationInput={mood:'thoughtful',duration:'normal',attention:'medium',context:'alone'};test('deterministic ranking and stable tie break',()=>{expect(rankMovies([movie(20),movie(10)],input,[]).map(x=>x.movie.tmdbId)).toEqual([10,20]);expect(rankMovies([movie(10),movie(20)],input,[]).map(x=>x.movie.tmdbId)).toEqual([10,20])});test('watched exclusion',()=>{const m=movie(1);expect(rankMovies([m],input,[{movie:m,watched:true,watchlist:false,favorite:false,updatedAt:'x'}])).toHaveLength(0)});test('explanation provenance',()=>{const result=rankMovies([movie(1,['Drama','Science Fiction'])],input,[])[0]!;expect(result.evidence.some(e=>e.code==='mood')).toBe(true);expect(result.explanation).toContain('düşündürücü yapısına');expect(result.explanation).not.toContain('AI')});
