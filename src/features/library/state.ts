import type { LibraryEntry,Movie,PersistedKareState,RecommendationRecord } from '../movies/domain';
export const EMPTY_STATE:PersistedKareState={version:1,library:[],recommendations:[]};
export type LibraryAction={type:'hydrate';state:PersistedKareState}|{type:'watchlist'|'watched'|'favorite';movie:Movie;at:string}|{type:'recommendation';record:RecommendationRecord};
function current(state:PersistedKareState,movie:Movie,at:string):LibraryEntry{return state.library.find(e=>e.movie.tmdbId===movie.tmdbId)??{movie,watchlist:false,watched:false,favorite:false,updatedAt:at}}
function replace(state:PersistedKareState,entry:LibraryEntry):PersistedKareState{return{...state,library:[...state.library.filter(e=>e.movie.tmdbId!==entry.movie.tmdbId),entry].sort((a,b)=>b.updatedAt.localeCompare(a.updatedAt))}}
export function sanitizeState(value:unknown):PersistedKareState{if(!value||typeof value!=='object')return EMPTY_STATE;const c=value as Partial<PersistedKareState>;if(c.version!==1||!Array.isArray(c.library)||!Array.isArray(c.recommendations))return EMPTY_STATE;const map=new Map<number,LibraryEntry>();for(const e of c.library)if(e?.movie&&Number.isInteger(e.movie.tmdbId))map.set(e.movie.tmdbId,e);return{version:1,library:[...map.values()],recommendations:c.recommendations.slice(0,50)}}
export function kareReducer(state:PersistedKareState,action:LibraryAction):PersistedKareState{if(action.type==='hydrate')return sanitizeState(action.state);if(action.type==='recommendation')return{...state,recommendations:[action.record,...state.recommendations].slice(0,50)};const e=current(state,action.movie,action.at);if(action.type==='watchlist')return replace(state,{...e,movie:action.movie,watchlist:!e.watchlist,updatedAt:action.at});if(action.type==='watched')return replace(state,{...e,movie:action.movie,watched:true,watchlist:false,updatedAt:action.at});return replace(state,{...e,movie:action.movie,favorite:!e.favorite,updatedAt:action.at})}
